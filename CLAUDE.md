# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

바탕화면에 떠 있는 한국 주식 시세 위젯. 프레임 없는 반투명 tkinter 창이 장중 7초마다
시세를 갱신하고, 가격 조건이 맞으면 카카오톡으로 알림을 보낸다.

## 명령

```bash
python stock_widget.pyw --selftest-alert   # 판정·주기·출처 99개 케이스 검사 (장 시간 무관)
python stock_widget.pyw --reset            # 창을 화면 밖에서 잃어버렸을 때 위치 초기화
python stock_widget.pyw                    # 위젯 실행
```

`--selftest-alert` 가 이 저장소의 유일한 자동 테스트다. 판정 로직을 건드렸으면 반드시 돌린다.
`selftest_alert()` 안에 케이스를 추가하는 것이 테스트를 늘리는 유일한 방법이다 — pytest 같은
프레임워크는 쓰지 않는다.

**반드시 `python` 으로 실행한다. `pythonw` 는 stdout 이 없어 테스트 결과가 안 보인다.**
평소 사용자는 `주식 위젯.lnk` 로 띄운다(`.pyw` 확장자가 이 PC에 연결돼 있지 않다).

## 절대 조건

- **표준 라이브러리만.** `pip install` 대상이 하나도 없어야 한다. 사용자가 파일 몇 개를
  바탕화면에 두고 쓰는 물건이다. 새 의존성을 제안하지 말고 stdlib 로 해결한다.
- **콘솔이 없다.** `.pyw` 라 `print` 도 traceback 도 사용자에게 안 보인다. 따라서
  - 사용자에게 알릴 오류는 반드시 tkinter 다이얼로그로 띄운다 (`load_config` 가 본보기)
  - 사후 진단은 `log_alert()` → `alerts.log` 가 유일한 통로다
  - 삼켜도 되는 예외만 `except: pass` 로 넘긴다. 위젯이 조용히 안 뜨는 것이 최악이다

## 구조

| 파일 | 역할 |
|---|---|
| `stock_widget.pyw` | 전부 (UI · 시세 · 판정 · 발송). 2600줄 단일 파일 |
| `kakao.py` | 카카오 OAuth 토큰 수명 관리 + 전송. **tkinter 를 모른다** |
| `kis.py` | 한국투자증권 OAuth 토큰 수명 관리 + 현재가 조회. **tkinter 를 모른다** |

사용자가 직접 고치는 파일: `config.json` · `.env`(KIS 앱키)

자동 생성 파일: `config.json`(설정) · `stocks.json`(KRX 종목 캐시, 30일) ·
`kakao_token.json`·`kis_token.json`(**자격증명**) · `alert_state.json`(급변동 하루 1회 상태) ·
`alerts.log`

### 데이터 흐름

```
Poller(스레드) ──fetch_kis / fetch_naver / fetch_yahoo──┐
                                                       ├─→ queue ──→ _drain_queue(메인)
Notifier(스레드) ──kakao.send_text─────────────────────┘                  │
       ↑                                                                  ↓
       │                                                          AlertEngine.feed
       │                                                                  │
       └────────────────── notifier.send(ev) ←── _on_alert ←──────────────┘
                                              └→ Toast (화면 팝업, 메인 스레드)
```

**tkinter 는 스레드 안전하지 않다.** 워커 스레드는 UI 를 절대 건드리지 않고 결과를
`self.q` 에 넣기만 한다. `Notifier` 도 전송 결과를 `("sent", ...)` 로 같은 큐에 넣어
메인 스레드가 받는다. 새 백그라운드 작업을 추가할 때 이 패턴을 따른다.

### quote 딕셔너리 — 모듈 사이의 계약

`fetch_naver` 와 `fetch_yahoo_one` 과 `_kis_quote` 는 **모양이 같은** 딕셔너리를 내야 한다.
필드를 추가하면 세 곳 다 고치고, `selftest_alert()` 의 `quote()` 헬퍼도 맞춘다
(셀프테스트가 `set(q) == set(quote(100))` 으로 세 곳의 키가 같은지 본다).

```python
{"code", "name", "price", "ratio",   # ratio 는 항상 절댓값
 "direction",     # 1 상승 / 0 보합 / -1 하락
 "limit",         # 1 상한가 / 0 아님 / -1 하한가 / None 판단 불가(Yahoo·KIS 부호 공백)
 "volume",        # 누적 거래량. 적응형 주기용. None 판단 불가(Yahoo)
 "market_open", "delayed"}
```

`limit` 은 네이버의 `compareToPreviousPrice.code`(`1`상한 `2`상승 `3`보합 `4`하한 `5`하락)에서
온다. **등락률로 상한가를 짐작하면 안 된다** — 가격제한폭이 늘 ±30% 인 것이 아니어서
(신규상장 첫날, 정리매매 등) 실측으로 +62% 인데 상한가가 아닌 종목이 있었다.

## AlertEngine — 판정

세 종류를 판정한다. 어려운 부분은 조건식이 아니라 **"언제 판정하지 않을지"** 다.
새 조건을 추가할 때 같은 방어를 빠뜨리지 않는다.

| 조건 | 재발동 | 상태 보관 |
|---|---|---|
| 목표가 `above`/`below` | 1회 발동 후 config 에서 그 키를 지운다 | 없음(config 자체가 상태) |
| `burst` 급변동 | 종목별 하루 1회 | `alert_state.json` |
| `limit` 상·하한가 | 전이마다 (하루 제한 없음) | 메모리 |

**판정을 건너뛰어야 하는 상황**

- `market_open == False` — 종가가 고정이라 판정할 값이 없다
- `delayed == True` (Yahoo 폴백) — 15분 지연 시세로 "지금"을 말할 수 없다.
  `limit` 은 아예 `None` 이라, 이걸 0으로 읽으면 "상한가 풀림"을 지어내게 된다
- 샘플이 `BURST_GAP_RESET`(60초) 이상 끊겼다 돌아옴 — 절전 복귀·네트워크 단절 후
  첫 샘플이 "5분 만에 3% 급등"으로 오판된다. 링버퍼를 통째로 비운다
- **첫 관측은 기준점으로만 쓴다.** 이미 상한가인 종목에 알림을 켰다고 즉시 울리면 안 된다.
  같은 이유로 장 마감 시 `limit` 기준점을 버려서, 상한가로 마감한 다음 날 아침에
  가짜 "풀림"이 나가지 않게 한다
- 상·하한가 전이는 `LIMIT_CONFIRM`(2) 회 연속 관측돼야 인정한다. 호가 한 틱이 오가며
  도배하는 것을 막는다

같은 "첫 관측은 기준점" 원칙이 적응형 주기(`Poller._saw_trade`)에도 있다. 종목을 새로
추가한 순환에서 그 종목의 누적 거래량을 "늘었다"고 읽으면 곧바로 하한까지 당겨진다.

시각은 전부 `time.monotonic()`. 절전 복귀 때 벽시계는 점프한다.

## 손대기 전에 알아야 할 것

**DPI 선언은 `import tkinter` 보다 먼저**, 파일 맨 위 `ctypes` 블록에서 해야 한다.
퍼모니터 v2 는 쓰지 않는다 — tkinter 8.6 이 `WM_DPICHANGED` 를 처리하지 못해 배율이 다른
모니터로 드래그하면 창이 깨진다.

**다중 모니터 좌표는 `virtual_screen_rect()` 로만 판정한다.** `winfo_screenwidth()` 는
주 모니터만 보므로 보조 모니터의 창을 매번 "화면 밖"으로 오판하고, 보조 모니터가 왼쪽에
있으면 정상 좌표가 음수라 "음수면 화면 밖" 검사도 틀린다.

**`config.json` 은 사용자가 직접 고치는 파일이다** (README 가 그렇게 안내한다).
따라서 `_merge_defaults` / `_clean_alerts` / `_clean_burst` / `_clean_holdings` 가 모든
값의 형태를 강제한다.
설정 항목을 추가하면 `DEFAULT_CONFIG` 와 이 검증 함수들을 같이 고친다. 못 읽는 값은
예외를 던지지 말고 버린다. BOM 때문에 읽기는 항상 `utf-8-sig`.

**`kakao.refresh()` 는 응답에 `refresh_token` 이 있을 때만 갱신한다.** 카카오는 잔여
유효기간이 1개월 미만일 때만 새로 내려준다. 없다고 기존 것을 지우면 두 달 뒤 알림이
조용히 멎는다.

**수익률(`holdings`)은 표시 전용이다.** `code -> {"cost", "qty"}` 이고 알림 판정에는
쓰지 않는다. 평단가는 사용자가 쥔 사실이라 검증할 방법이 없어서 — 잘못 넣은 값으로
카카오톡 알림까지 나가면 되돌릴 수 없다. 줄 색도 오늘 등락 기준 그대로 둔다(수익률은
괄호 안 부호로만 말한다). 수량은 선택이고, 없으면 툴팁에서 평가손익 줄을 생략한다.

**`sys.dont_write_bytecode = True`** 는 바탕화면 폴더에 `__pycache__` 가 생기지 않게 하려는
의도적 설정이다. 지우지 않는다.

**윈도우 알림은 tkinter 로 직접 그린다**(`Toast`). PowerShell `Windows.UI.Notifications` 는
실행 정책에 걸리고 콘솔 창이 튀어서 "콘솔 없는 위젯"이라는 전제와 맞지 않는다.

## 수동 점검

`--selftest-alert` 로 못 잡는 UI·연동 경로는 임시 스크립트로 확인한다.
`StockWidget` 을 만들면 `_place_initial` 이 곧바로 `save_config` 를 호출하므로,
**테스트 전에 쓰기 경로를 반드시 갈아끼운다. 안 그러면 사용자의 실제 설정이 덮인다.**

```python
m.CONFIG_PATH = SCRATCH / "config.json"
m.ALERT_STATE_PATH = SCRATCH / "alert_state.json"
m.ALERTS_LOG_PATH = SCRATCH / "alerts.log"
m.kakao.TOKEN_PATH = SCRATCH / "kakao_token.json"
m.kis.TOKEN_PATH = SCRATCH / "kis_token.json"
```

다이얼로그는 `parent.wait_window()` 로 블로킹하므로 `root.after()` 로 닫아 준다.
카카오 연동 마법사는 브라우저에서 오류가 나면 리다이렉트가 아예 오지 않아 위젯이 2분을
기다리기만 한다 — 그래서 KOE205/KOE006/KOE101 대응표를 마법사 안에 직접 넣어 두었다.

## 시세 출처와 갱신 주기

출처는 **KIS → 네이버 → Yahoo** 순으로 시도하고 처음 성공한 것을 쓴다(`Poller._sources`).
KIS 는 연동돼 있을 때만 목록에 들어간다(`kis.is_linked()`, 30초 캐시 — `wake()` 가 무효화한다).
Yahoo 는 폴백이다(**한국 주식 15분 지연**, `delayed=True`, 가격 옆에 `*` 표시).
Yahoo URL 은 `range=1d` 여야 `previousClose` 가 전일 종가가 된다 — `5d` 로 하면 등락률이
엉뚱해진다(실측 4.79% 대신 16.05%).

**KIS 는 적응형을 끈다**(`_next_interval(..., adaptive=False)`). 적응형은 "한도를 모르는
서버에 빈 요청을 덜 보내자"는 타협인데, 공식 API 는 한도가 공개돼 있어(실전 초당 20건)
그 타협을 할 이유가 없다. 그래서 KIS 로 돌 때는 `interval_open` 이 **하한이 아니라 그대로
주기**다. 설정이 없으면 7초. 같은 메뉴가 소스에 따라 다른 뜻이 되므로 라벨도 바뀐다
("최고 1초" ↔ "1초 고정"). 적응형을 꺼도 `_saw_trade` 는 계속 불러 기준점을 갱신한다 —
KIS 가 잠깐 실패해 네이버로 떨어지는 순환에서 첫 관측을 "방금 체결됐다"로 읽지 않기 위해서다.

KIS 로 주의할 것:

- **토큰 발급 때마다 사용자 폰으로 알림톡이 간다.** 그래서 `kis_token.json` 에 반드시
  캐시한다. 재발급은 1분 1회 제한(EGW00133)이라 `_MIN_ISSUE_GAP` 으로 스스로 간격을 지킨다
- **앱키의 정답은 `.env` 다.** `env_keys()` 에 유효한 값이 있으면 `kis_token.json` 에 저장된
  앱키보다 우선하고, 둘이 다르면 `get_token()` 이 `.env` 쪽으로 토큰을 새로 받는다. 두 곳이
  앱키를 쥐면 "고쳤는데 왜 안 바뀌지"가 되므로, `.env` 가 유효할 때 UI 는 입력창을 아예
  띄우지 않는다(`_build_source_menu` / `_setup_kis` 의 가드). 템플릿을 그대로 둔 것은
  `_is_placeholder` 가 한글로 걸러낸다 — 안내문이 한글인 것은 그래서 의도다
- **`EGW00201`(초당 거래건수 초과)은 호출 간격으로 못 피한다.** 실측에서 0.05초 간격 5회는
  멀쩡했고 0.5초 간격에서 오히려 났다. 산발적이라 `_MIN_CALL_GAP` 을 키우는 것은 답이
  아니고, `_authed_get` 이 `RATE_RETRY_WAIT` 만큼 쉬었다 한 번 더 건다. 그래도 실패하면
  그 순환만 네이버로 넘어간다. **유량 초과는 HTTP 200 에 `rt_cd` 로 실려 오므로** 재시도
  판정이 `rt_cd` 검사를 감싸야 한다(`once()` 안에 함께 둔 이유)
- `inquire-price` 는 **한 번에 한 종목**이다. 네이버가 한 요청으로 주던 것을 종목 수만큼
  나눠 부른다. `kis._space_calls()` 가 초당 한도를 지킨다
- **응답에 한글 종목명이 없다**(`bstp_kor_isnm` 은 업종명이다). 이름은 `_NAME_CACHE`
  (KRX `stocks.json` 캐시)에서 붙인다. 1초마다 190KB 를 다시 읽을 수 없어서 파일이 아니라
  이 딕셔너리가 조회 경로다
- **응답에 `marketStatus` 같은 것도 없다.** 장 구분은 `kis.session_now()` 가 PC 시계로
  판정하고, 휴장일만 공식 API(`chk-holiday`)로 하루 한 번 확인해 메모리에 기억한다.
  확인에 실패하면 **열린 쪽으로** 본다 — 휴장일을 개장으로 잘못 봐도 값이 고정이라 알림이
  잘못 나가지 않지만, 개장일을 휴장으로 보면 알림이 통째로 멎는다
- `prdy_vrss_sign` 은 네이버 `compareToPreviousPrice.code` 와 같은 체계(1상한 2상승 3보합
  4하한 5하락)다. 부호가 **비어 오면 `limit` 은 0 이 아니라 `None`** 이다. 0 으로 읽으면
  가짜 "상한가 풀림"을 지어낸다
- 연동돼 있는데 계속 실패하면 조용히 네이버로 돈다. 그 사실을 `Poller.kis_error` 에 남기고
  툴팁과 메뉴가 보여준다. 상태는 폴링 스레드 한 곳에만 둔다(`StockWidget.kis_broken` 은
  그것을 읽는 property 다)

**실측(2026-09-15 장중, 1종목).** 1초 설정으로 20초를 돌려 표본 18개, 오류 0건, 주기는
내내 1.0초로 유지됐다(실제 간격은 왕복 시간이 더해져 평균 1.15초). 누적 거래량이 실제로
늘어난 것은 17번 중 10번 — 나머지 7번은 빈 요청인데, 네이버였다면 적응형이 물러났을
구간이다. 같은 시각 KIS 와 네이버의 가격·등락률은 정확히 일치했다(13,180원 / 16.64%).

아래는 **네이버로 돌 때**의 이야기다.

장중 주기는 **적응형**이다(`Poller._next_interval`). `config["interval_open"]` 은 고정
주기가 아니라 **하한**이다 — 체결이 실제로 있었으면 그 값으로 당기고, 없으면 `ADAPT_STEP`
(1초)씩 물러나 서버 권장값(`pollingInterval`, 7000ms)까지 되돌아간다. 설정이 없으면
권장값 고정이라 기존 동작 그대로다. 판단 근거는 누적 거래량(`quote["volume"]`)의 증가다.

**체결시각이 새로 왔다고 새 데이터인 것이 아니다.** 1초 간격 20회 실측에서
`localTradedAt` 은 매번 바뀌었지만(요청 시각을 따라올 뿐이다) 누적 거래량이 실제로 늘어난
것은 6회뿐이었다. 나머지 14회는 같은 값을 다시 받은 빈 요청이다. 종목의 체결 빈도가
상한이지 폴링 주기가 상한이 아니다. 적응형은 이 빈 요청을 줄이려는 것이다.

주기를 줄이는 것은 성능 문제가 아니라 서버가 권하는 주기를 무시하는 문제다. 네이버는
레이트리밋을 공개하지 않고 응답에 한도 헤더(`X-RateLimit-*`, `Retry-After`)도 없어
차단이 가까워졌는지 알 방법이 없다. `ETag`·`Last-Modified` 가 없어 조건부 요청도 못 쓰고,
`Connection: close` 라 연결 재사용도 서버가 거부한다. 빈 요청을 줄이는 것 말고 깎을 여지가
없다는 뜻이다. 차단되면 15분 지연인 Yahoo 로 떨어진다. 차단 없이 1초를 원하면 증권사 공식
API 로 가는 것이 정공법이고, 그래서 KIS 를 붙였다(무료·실전 초당 20건).

**하한을 낮출수록 요청은 는다.** 활발한 종목은 대부분 하한에 머물기 때문이다. 하한 1초는
장중 약 11,700회로 1초 고정(23,400회)의 절반이지만 3초 고정(7,800회)보다는 많다.
하한 3초는 약 6,000~6,700회로 3초 고정보다 적다.

## 장 구분 — 15:30 이 끝이 아니다

**2026-09-14 부터 KRX 애프터마켓(16:00~20:00)이 생겼다.** 종전 시간외단일가
(16:00~18:00, 10분 단위)를 20:00 까지 늘리고 접속매매로 바꾼 것이다. 전 종목이 대상이고
(ETF·ETN 제외), **접속매매라 가격이 정규장처럼 계속 움직인다.**

`kis.session_now()` 가 네 가지를 돌려준다.

| 구간 | 시각 | 가격 | 주기 |
|---|---|---|---|
| `regular` | 09:00~15:30 | 움직인다 | 사용자 설정 |
| `between` | 15:30~16:00 | **종가 고정**(시간외종가 매매) | `INTERVAL_IDLE`(10초) |
| `after` | 16:00~20:00 | 움직인다 | 사용자 설정 |
| `closed` | 그 외·주말·휴장일 | — | `INTERVAL_CLOSED`(60초) |

**`market_open` 은 `session != "closed"` 다. 정규장만 True 로 두면 안 된다.**
실측(2026-09-15)에서 네이버 `marketStatus` 는 15:30 이 지나도 계속 `OPEN` 이었다. quote
계약상 두 소스가 같은 뜻을 내야 하는데, KIS 만 15:30 에 끊으면 시세 출처가 바뀌는 것만으로
알림이 켜졌다 꺼졌다 한다. 같은 날 애프터마켓 실측에서 16:01~16:08 에 6,930 → 7,100 →
7,000 (2.5% 폭)으로 움직였고 KIS `J`(KRX)가 그대로 반영했다 — 그 구간을 "마감"으로 보면
목표가 알림이 조용히 안 나간다. 등락률(`prdy_ctrt`)도 전일 종가 대비로 정상 갱신된다.

`between` 만 따로 두는 이유는 그 30분 동안 값이 종가에 묶여 있어서다. 네이버가 OPEN 을
주니 "마감"으로 접지는 못하지만 1초로 물어볼 이유도 없다. 이 구간에서도 `_saw_trade` 는
불러 기준점을 갱신한다 — 16:00 에 애프터마켓이 열리는 첫 관측을 "방금 급증"으로 읽지
않기 위해서다.

시장 구분 코드는 `J`(KRX) 를 쓴다. 실측에서 `UN`(통합)과 값이 같았고, `NX`(NXT)는 이
종목에서 빈 응답이었다(NXT 는 600여 종목만 다룬다).

주기를 건드릴 때 함께 봐야 하는 곳:

- `is_stale()` 의 임계는 `max(주기 x STALE_FACTOR, STALE_MIN)`. 바닥값이 없으면 1초 주기에서
  3초만 늦어도 회색이 되어 표시가 깜빡인다
- 폴링은 "받고 → 기다리고"의 순차 구조라 주기가 `FETCH_TIMEOUT` 보다 짧아도 요청이 겹치지
  않는다. 느린 응답 한 번이 그 순환만 늦춘다
- 장 마감 중에는 사용자 설정과 **출처와도** 무관하게 `INTERVAL_CLOSED`(60초)를 쓴다
- 장 구분은 `Poller._session()` 이 `kis.session_now()` 에 물어본다. 시계 계산을 위젯에
  다시 쓰지 않는다 — 장 시간표를 아는 곳은 `kis.py` 하나여야 한다(휴장일 API 가 거기 있다)
